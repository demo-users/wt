<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Chess Board</title>
    <style>
        table {
            border-collapse: collapse;
            margin: 20px auto;
        }
        td {
            width: 50px;
            height: 50px;
        }
        .black {
            background-color: black;
        }
        .white {
            background-color: white;
        }
    </style>
</head>
<body>

<?php
echo "<table>";
for ($row = 0; $row < 8; $row++) {
    echo "<tr>";
    for ($col = 0; $col < 8; $col++) {
        // If sum of row and column is even, color white, else black
        $class = (($row + $col) % 2 == 0) ? "white" : "black";
        echo "<td class='$class'></td>";
    }
    echo "</tr>";
}
echo "</table>";
?>

</body>
</html>
