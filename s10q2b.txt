import numpy as np
from itertools import combinations
points = np.array([
    [1, 2],
    [3, 4],
    [5, 6],
    [7, 8]
])
sum_manhattan = 0
for p1, p2 in combinations(points, 2):
    sum_manhattan += np.sum(np.abs(p1 - p2))
print("Sum of Manhattan distances between all pairs:", sum_manhattan)

